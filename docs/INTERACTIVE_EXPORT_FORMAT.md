# Interactive MVP export format

The static interactive MVP supports local browser calculation and export without Next.js, npm build, backend services, login, or database state.

Export modes: JSON result export, CSV result export, and pipeline JSON copy.

scripts/convert_interactive_export.py converts pipeline JSON into a scenario folder with case_description.md and vulnerabilities.csv.
